query SetQuery($setID: ID!) {
  set(id: $setID) {
    id
    setGamesType
    round
    phaseGroup {
      bracketType
      rounds {
        number
        bestOf
      }
    }
    games {
      id
      orderNum
      winnerId
    }
  }
}
